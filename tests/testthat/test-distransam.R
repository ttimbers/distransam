context("distransam")
library(gapminder)

test_that("randomly_sampled_dataframe is the same width and types as input data frame", {
})

test_that("if sample_var = NULL, N for each group should be equal in randomly_sampled_dataframe",{
  
})

test_that("if sample_var = !NULL, N for each group should be equal in randomly_sampled_dataframe", {
  
})

test_that("if sample_var = NULL, then N must be > minimum # individuals in smallest group", {
  
})
  
test_that("if sample_var = !NULL, then N/# of samples must be > minimum # individuals in 
  smallest sample within all groups", {
    
  })